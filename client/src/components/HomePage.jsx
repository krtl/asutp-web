import React from 'react';
import { Card, CardTitle } from '@mui/material';


const HomePage = () => (
  <Card className="container">
    <CardTitle title="ASUTP-WEB home" subtitle="Please login." />
  </Card>
);

export default HomePage;
